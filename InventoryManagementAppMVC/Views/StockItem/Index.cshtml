@using System.Text.Json;
@using System.Text.Json.Serialization;
@using InventoryManagementAppMVC.Enum;
@model ResponsePagination

<!-- Striped Rows -->
<h5 class="fw-bold"><span class="text-muted fw-light">Truck /</span> Basic Tables</h5>
<div class="card">
    <div class="table-responsive text-nowrap">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th></th>
                    <th>ID</th>
                    <th>Item Name</th>
                    <th>Type</th>
                    <th>Quantity</th>
                    <th>Quantity State</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">

                @{
                    int i = 1;
                    foreach (JsonElement item in Model.Entities)
                    {
                        StockItemVM stock = JsonSerializer.Deserialize<StockItemVM>(item, new JsonSerializerOptions
                        {
                            PropertyNameCaseInsensitive = true,
                            DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull
                        });

                        <tr>
                            <td>@i</td>
                            <td>@stock.StockItemID</td>
                            <td>@stock.Name</td>
                            <td>@stock.Type</td>
                            <td>@stock.Quantity</td>
                            <td>@stock.QuantityState</td>
                            <td>
                                <div class="dropdown">
                                    <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                        <i class="bx bx-dots-vertical-rounded"></i>
                                    </button>
                                    <div class="dropdown-menu">
                                        <a class="dropdown-item" href="javascript:void(0);"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                        <a class="dropdown-item" href="javascript:void(0);"><i class="bx bx-trash me-1"></i> Delete</a>
                                    </div>
                                </div>
                            </td>
                        </tr>
                        i++;
                    }
                }

            </tbody>
        </table>
    </div>

    <!-- Pagination -->
    <div class="demo-inline-spacing">
        <nav aria-label="Page navigation">
            <ul class="pagination justify-content-center">
                @{
                    int start = 1;
                    int max = Model.Pages;

                    if (Model.CurrentPage == 1)
                    {
                        start = 1;
                    }
                    else if (Model.Pages - Model.CurrentPage < 4 && Model.CurrentPage > 5)
                    {
                        start = Model.Pages - 4;
                    }

                    if (Model.Pages > 5)
                    {
                        max = start + 4;
                    }

                    for (int j = start; j < max + 1; j++)
                    {
                        if (j == Model.CurrentPage)
                        {
                            <li class="page-item active">
                                <a class="page-link">@j</a>
                            </li>
                        }
                        else
                        {
                            <li class="page-item"><a class="page-link" asp-controller="StockItem" asp-action="Index" asp-route-page="@j">@j</a></li>
                        }
                    }
                }
            </ul>
        </nav>
    </div>
</div>
<!--/ Striped Rows -->